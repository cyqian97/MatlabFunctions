function image_points = calibrateCamera(frames, board_size, square_size_in_mm)
%calibrateCamera compute the camera intrinsics on input image frames.
%  Each frame must contain only 1 checkerboard.
image_points = zeros((boardSize(1)-1)*(boardSize(2)-1),2,size(frames,4));

for i = 1:



end